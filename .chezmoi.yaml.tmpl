data:
    environment: {{ promptString "Enter environment (host/sbx)" }}

edit:
    command: "nvim"
    apply: true

git:
    autocommit: true
    autopush: true

diff:
    pager: "bat --language=diff"

pager: "bat"
